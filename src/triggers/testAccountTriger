trigger testAccountTriger on Account (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
   if(Trigger.isBefore){
      /*for(Account a: trigger.new){
          a. Name = 'Test Account';
      }*/
   }
}
